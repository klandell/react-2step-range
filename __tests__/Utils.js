import {
  findChildrenByType,
  findChildByType,
  renderChildOfType,
  shallowDiff,
  capitalize,
  calculateInitialState,
  calculateNextState,
  calculateNumericValue,
  findClosestIdx,
} from '../src/Utils';

test('Placeholder test', () => {
  expect(1).toBe(1);
});
